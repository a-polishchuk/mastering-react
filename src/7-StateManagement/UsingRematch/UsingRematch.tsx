import { ChapterWrapper } from 'components';

export function UsingRematch(): JSX.Element {
  return (
    <ChapterWrapper title="Rematch" subtitle="State management">
      <div>Rematch</div>
    </ChapterWrapper>
  );
}
